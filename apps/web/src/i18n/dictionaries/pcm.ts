/** Nigerian Pidgin (Naijá) — preview. Pidgin is English-based, so it derives
 * from English; reviewed phrasings can be layered in later. */

import { type Dictionary, en } from './en';

export const pcm: Dictionary = {
  ...en,
};
